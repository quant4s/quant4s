/**
  *
  */
package quanter.securitySelection

import scala.collection.mutable.ArrayBuffer

/**
  *
  */
object SelectiorExtension {

//  implicit class SelectorExt(arr: Selector) {
//    def filter (cmp: Instrument => Boolean) = {
//      new Selector(arr.pool.filter( e => cmp.apply(e)))
//    }
//  }
}
